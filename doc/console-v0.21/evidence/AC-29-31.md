# P0-074 故障与数据完整性演练

- 日期：2026-09-09
- 命令：`vitest run tests/console/transaction.test.ts tests/console/context-output.test.ts tests/console/indexing.test.ts tests/console/obsidian-transaction-port.test.ts`
- 结果：退出码 0；4 个文件、21 项测试通过

| 注入故障 | 稳定结论 | 精确测试 |
|---|---|---|
| 缓存内容损坏或 schema 不兼容 | 忽略缓存并允许从 Markdown 重建，不改 Markdown | `indexing.test.ts` cache round-trip |
| 计划后文件被并发修改 | 返回 `CONCURRENT_MODIFICATION`，写入数为 0 | `transaction.test.ts` optimistic conflict |
| 第二个文件写入失败 | 已写文件按逆序恢复原文 | `transaction.test.ts` injected failure |
| 补偿前作者再次修改文件 | 停止覆盖，保留作者内容并输出精确人工恢复项 | `transaction.test.ts` external change during compensation |
| delete/move 后索引发布失败 | 删除文件与移动路径均恢复 | `transaction.test.ts` index publication failure |
| Context MD 成功而 JSON 写入失败 | MD 补偿到上一版，上一套 MD+JSON 完整保留 | `context-output.test.ts` second write failure |

人工恢复边界：出现 `manual_recovery_required` 后，按 recovery report 的 path、operation、reason 处理；不得从审计日志恢复正文，因为审计不保存正文全文。
