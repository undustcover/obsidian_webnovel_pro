# P0-083D 模板页 P0 范围

日期：2026-09-11  
状态：完成。

## 实现

- 合并规范 `template` 索引记录与既有章节模板设置，按路径去重。
- 显示模板来源、有效/坏路径状态和原文入口；坏路径按钮禁用但不静默丢失。
- 创建入口调用既有 `create-next-chapter` 命令，明确不提供模板设计器或新增字段。

## 验证

`wave8a-queries.test.ts` 覆盖索引/设置合并、有效路径和坏路径；路由测试保证模板入口不再使用全实体 fallback。

## 回滚点

页面可降级为只读模板列表，不影响既有模板设置与创建命令。
