# P0-083A 视图管理与小说总览

日期：2026-09-11  
状态：完成。

## 实现

- `ControlOverviewQuery` 从单一当前 snapshot 汇总项目、版本、总记录和实体计数。
- 视图管理页按声明式导航列出入口与职责内计数，可直接导航。
- 小说总览复用 `DashboardQuery` 的焦点、游标、健康和行动计算，仅补充六层叙事计数，未复制领域规则。
- 当前阶段/当前任务在 Wave 8A 提供只读投影，并明确将写入操作留给 Wave 8B。

## 验证

`wave8a-queries.test.ts` 覆盖有 snapshot 的计数/结构；无 snapshot 由查询返回明确零值。完整测试与覆盖率闸门通过。

## 回滚点

两个只读页面及查询可独立撤回，不影响 Dashboard。
