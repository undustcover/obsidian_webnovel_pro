# P0-084C 人物与道具详情

日期：2026-09-11  
状态：完成。

## 实现

- 人物详情展示核心字段、关系、事件经历、章节经历及坏位置冲突证据。
- 道具详情展示核心字段、关系、当前持有者、完整持有历史、事件/章节及多持有冲突证据。
- 关联事件/章节使用原生按钮进入同一详情，不实现规范外的复杂时态推理。

## 验证

`wave8a-pages.test.ts` 与 `entity-centers.test.ts` 覆盖正常关联、缺关系、多持有、坏位置，并断言实际证据文本而非仅计数。5/5 相关测试通过。

## 回滚点

可退回只读摘要；CharacterCenterQuery/ItemCenterQuery 和索引不受影响。
